[pytest]
markers =
    url_check: marks tests that verify target URLs (deselect with '-m "not url_check"')
    integration: marks integration 'smoke' tests that hit live URLs (deselect with '-m "not integration"')
